/*
import model.Admin;
import model.User;
import org.junit.Assert;

import java.util.ArrayList;

public class AdminTest {
    public void addNewAdmin() {
        Admin admin = new Admin("firstName", "lastName",
                "userName", "password",
                "example@exp.exp", "09000000000");
        ArrayList<User> admins = new ArrayList<>();
        Admin.addAdmins(admins);

        Assert.assertEquals(Admin.getAdmins().get("userName").getUsername(), "userName");
        Assert.assertEquals(Admin.getAdmins().size(), 1);
    }
}
*/
